# `pairing/`

`PairingService` orchestrates per-kind `IPairingDriver` implementations,
the credential store, and the device repository.

PR-5 introduces `PairingService` and `androidTvPairingDriver`.
